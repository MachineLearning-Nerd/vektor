# Task 2.9 — v0.2.0 release readiness + Phase 3 handoff docs

**Phase**: 2 — Discovery + Chunking
**Task ID**: 2.9
**PRD reference**: Roadmap Stage 2 / `v0.2.0`
**Roadmap stage**: Stage 2 / `v0.2.0`
**Effort estimate**: M
**Depends on**: 2.8
**Blocks**: 3.*

## Objective

Prepare the `v0.2.0` source-release handoff after discovery and chunking are verified. This task updates release/tracker docs only; do not tag, push, or create a GitHub Release without explicit publish approval.

## Inputs (must exist before starting)

- Tasks 2.1 through 2.8 completed and marked done with commit hashes
- Green local verification on the final implementation commit
- Explicit approval before any irreversible publish action

## Outputs (must exist after completion)

- Release-readiness docs for `v0.2.0`; no tag or GitHub Release in this task
- Draft notes-only release text in `release-notes-v0.2.0.md`
- Tracker docs updated for Phase 2 completion and Phase 3 readiness
- Phase 3 readiness state documented before embedding/storage implementation begins

## Approach

- Re-run the phase-level verification against the final Phase 2 implementation.
- Update tracker docs in the same release-readiness change so phase status does not drift.
- Leave CI confirmation, tagging, pushing, and GitHub Release creation to a separate explicitly approved publish step.

## Acceptance criteria

- [ ] Tasks 2.1 through 2.8 are marked done with commit hashes
- [ ] `vektor index --dump-chunks src/` on Vektor itself prints non-trivial chunks
- [ ] Header preservation and max-line tests pass
- [ ] Tracker docs state that `v0.2.0` tag/GitHub Release are pending explicit approval
- [ ] `release-notes-v0.2.0.md` exists as a draft-only document
- [ ] Phase 3 docs state the next work is readiness/per-task expansion, not embedding implementation
- [ ] No tag or GitHub Release is created by this task

## Verification

```bash
cargo fmt --check
cargo clippy --locked --workspace --all-targets -- -D warnings
cargo test --locked --workspace
cargo build --locked
FAKE_HOME=$(mktemp -d)
HOME="$FAKE_HOME" USERPROFILE="$FAKE_HOME" ./target/debug/vektor index --dump-chunks src/
HOME="$FAKE_HOME" USERPROFILE="$FAKE_HOME" ./target/debug/vektor index src/
HOME="$FAKE_HOME" USERPROFILE="$FAKE_HOME" ./target/debug/vektor index src/
HOME="$FAKE_HOME" USERPROFILE="$FAKE_HOME" ./target/debug/vektor index --force src/
git diff --check
```

## Notes

- Post-publish status: the `v0.2.0` publish gate is complete privately and Phase 3 per-task files exist. Start Phase 3 implementation from those task files, not from the phase table.
- CI confirmation, tag push, and GitHub Release creation were intentionally omitted from this readiness task because they required separate publish approval.
